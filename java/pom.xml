<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.eobi</groupId>
  <artifactId>sentinel</artifactId>
  <version>0.1.4</version>
  <packaging>jar</packaging>

  <name>Nemesis Shield Sentinel SDK</name>
  <description>Nemesis Shield - Sentinel SDK for Java. Positive-security runtime protection: learns your
    app's normal behavior and blocks off-baseline requests (auth bypass, path traversal, scanners,
    unusual methods) in enforce mode, plus an OWASP-LLM prompt-injection guard backed by an embedded
    ML model (byte-identical across all Nemesis Shield SDKs). Servlet filter included. Fail-open,
    privacy-preserving, JDK 11+.</description>
  <url>https://github.com/eobi/nemesis_shield_sdks</url>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <name>Autogon Inc.</name>
      <email>support@nemesislabs.xyz</email>
      <organization>Nemesis Labs</organization>
      <organizationUrl>https://nemesislabs.xyz</organizationUrl>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:https://github.com/eobi/nemesis_shield_sdks.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/eobi/nemesis_shield_sdks.git</developerConnection>
    <url>https://github.com/eobi/nemesis_shield_sdks/tree/main/java</url>
  </scm>

  <properties>
    <maven.compiler.release>11</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>jakarta.servlet</groupId>
      <artifactId>jakarta.servlet-api</artifactId>
      <version>6.0.0</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
    </plugins>
  </build>

  <!-- Activate for publishing: mvn -Prelease deploy  (attaches sources+javadoc, GPG-signs,
       and uploads to the Central Portal). Local `mvn package` stays fast and unsigned. -->
  <profiles>
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>3.3.1</version>
            <executions>
              <execution>
                <id>attach-sources</id>
                <goals><goal>jar-no-fork</goal></goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>3.6.3</version>
            <executions>
              <execution>
                <id>attach-javadocs</id>
                <goals><goal>jar</goal></goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.1.0</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals><goal>sign</goal></goals>
                <configuration>
                  <gpgArguments>
                    <arg>--pinentry-mode</arg>
                    <arg>loopback</arg>
                  </gpgArguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>0.5.0</version>
            <extensions>true</extensions>
            <configuration>
              <publishingServerId>central</publishingServerId>
              <autoPublish>false</autoPublish>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
